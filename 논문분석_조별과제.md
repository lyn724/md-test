# 기계학습 조별과제 논문 분석 (지정 범위 기준)

분석 범위
- 논문 1: Pepino et al., arXiv:2104.03502, p.1~4 (약 2.5p), wav2vec 사용 근거
- 논문 2: Kundu et al., arXiv:2412.10011, p.1, 3~11 (약 10p), BiLSTM + Attention 구조
- 논문 3: Vaswani et al., arXiv:1706.03762, p.4~5 (약 2p), Multi-Head Attention 수식

---

## 1) 논문 1 (Pepino): wav2vec 사용 근거

### 핵심 요지
- SER 데이터셋이 작아 end-to-end 대형 모델 직접 학습이 어렵고, 전이학습이 필요하다고 제시.
- wav2vec 2.0을 feature extractor로 사용하고, 단일 층 대신 여러 층 출력을 가중합하는 방식 제안.

### 코드 연결 지점
- `Wav2VecExtractor` 초기화/고정(freeze) 여부
- `hidden_states` 전체를 받아 가중합 특징 생성
- `last_hidden_state` 단독 대비 all-layer fusion 비교 실험

### 수식/설계 포인트
- 레이어 가중합:
  - `f = (sum(alpha_i * f_i)) / (sum(alpha_i))`
  - `alpha`는 학습 가능한 파라미터

### 표/그래프 해석
- **Table 1**: Wav2vec2-PT + All layers가 IEMOCAP/RAVDESS에서 가장 높은 평균 recall 보고.
- **Figure 2**: 중간 레이어 가중치가 상대적으로 큼. 저자는 중간층이 SER에 유용한 문맥 정보를 더 잘 담는다고 해석.

---

## 2) 논문 2 (Kundu): BiLSTM + Attention 구조

### 핵심 요지
- 이중 채널 구조:
  - **ALFB**: 1D CNN + ECA-Net (채널 attention)으로 로컬 특징 강화
  - **GFB**: BiLSTM으로 전후 문맥 기반 글로벌 특징 추출
- 두 채널을 concat한 뒤 dense-softmax로 감정 분류.

### 코드 연결 지점
- ALFB 블록(Conv1D -> ECA -> BN -> MaxPool) 4개
- GFB 블록(BiLSTM -> BN -> Dropout) 2개
- 최종 concat + dense(32) + softmax

### 명시된 주요 하이퍼파라미터
- batch size 32, lr `1e-4`, epoch 100, CE loss
- ALFB filter: 256, 256, 256, 32
- GFB units: 512, 512

### 표/그래프 해석
- **Table 5 (5-fold CV)**: TESS 99.65, RAVDESS 94.88, BanglaSER 98.12, SUBESCO 97.94, Emo-DB 97.19.
- **Figure 8 (Confusion Matrix)**: 대각선이 높아 전반 분류 양호. SUBESCO에서 Disgust 혼동이 상대적으로 큼.
- **Figure 9 (학습/검증 곡선)**: train/val 정렬이 가까워 과적합이 크지 않다고 해석.
- **Figure 10 (ROC)**: 전반적으로 높은 분리 성능을 보고.
- **Table 11 (Ablation)**: ECA 또는 GFB 제거 시 정확도 하락 -> Attention + BiLSTM 기여를 정량 확인.

---

## 3) 논문 3 (Vaswani): Multi-Head Attention 수식

### 핵심 수식
- Scaled Dot-Product Attention:
  - `Attention(Q,K,V) = softmax(QK^T / sqrt(d_k))V`
- Multi-Head:
  - `head_i = Attention(QW_i^Q, KW_i^K, VW_i^V)`
  - `MultiHead(Q,K,V) = Concat(head_1, ..., head_h) W^O`

### 해석
- `sqrt(d_k)` 스케일링은 큰 차원에서 내적값 폭주를 줄여 softmax 학습 안정성 확보.
- 다중 head는 서로 다른 표현 서브공간에서 병렬로 관계를 학습해 단일 head보다 표현력이 높음.
- 논문 설정 예: `h=8`, `d_k=d_v=d_model/h=64`.

---

## 4) 조별과제 구현 요약

- **특징 추출 축(Pepino)**: wav2vec hidden layers 가중합을 기본 실험축으로 채택.
- **백본 축(Kundu)**: ALFB(로컬 attention) + GFB(BiLSTM 글로벌 문맥) 병렬 결합 구조 적용.
- **수식 축(Vaswani)**: attention core는 scaled dot-product + multi-head 수식 그대로 구현.

주의
- 논문 간 지표(accuracy vs average recall), 데이터셋, 분할 방식이 달라 수치 절대 비교는 제한적.
